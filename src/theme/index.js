export {
  DEFAULT_PALETTE_ID,
  PALETTE_STORAGE_KEY,
  palettes,
  isPaletteId,
  getPalette,
  listPalettes,
  resolveTokens,
  resolvePrintTheme,
  readStoredPaletteId,
  buildAllPalettesCss,
  getPaletteBootstrapScript,
} from "./registry";
